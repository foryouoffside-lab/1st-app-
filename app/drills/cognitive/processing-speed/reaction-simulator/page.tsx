import ReactionSimulatorWrapper from './ReactionSimulatorWrapper';

export const metadata = {
  title: 'Reaction Simulator | SkillDrills',
  description: 'Intercept falling targets at progressive speeds and train vertical visual tracking and hand-eye coordination.',
};

export default function ReactionSimulatorPage() {
  return <ReactionSimulatorWrapper />;
}
