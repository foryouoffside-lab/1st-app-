import ReactionTimeTestWrapper from './ReactionTimeTestWrapper';

export const metadata = {
  title: 'Reaction Time Test | SkillDrills',
  description: 'Measure your visual reaction latency in milliseconds and train to improve it.',
};

export default function ReactionTimeTestPage() {
  return <ReactionTimeTestWrapper />;
}
